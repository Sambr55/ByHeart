import { Waitlist } from '@/components/Waitlist'

export const metadata = {
  title: 'DUB — European Portuguese, through what you already know',
  description:
    'Every big app teaches you Brazilian Portuguese. DUB teaches the European one, through the films and music already in your head.',
}

export default function WaitlistPage() {
  return <Waitlist />
}

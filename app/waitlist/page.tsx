import { Waitlist } from '@/components/Waitlist'

export const metadata = {
  title: 'DUB — European Portuguese, through what you already know',
  description:
    'Every big app teaches you Brazilian Portuguese. DUB teaches the European one — and starts by getting you talking about yourself.',
}

export default function WaitlistPage() {
  return <Waitlist />
}
